module.exports = {
    filenameHashing: false,

    // outputDir should be Laravel public dir
    outputDir: '../../public/arm',
    publicPath: '/',

    // for production we use blade template file
    indexPath: 'index.html',
}
